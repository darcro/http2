/**
 * Incremental HTTP/2 frame extraction from ordered, deduplicated, contiguous
 * TCP payload bytes. TCP reconstruction, direction tracking, and TLS
 * processing remain external.
 */
package dev.darcro.extract;
