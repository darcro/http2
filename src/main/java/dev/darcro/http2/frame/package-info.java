/**
 * Evidence-preserving observation and strict parsing of exact HTTP/2 frame
 * candidates, frame value types, byte views, and stateless client-preface
 * validation.
 *
 * <p>This package does not perform stream reconstruction and does not depend on
 * or implicitly invoke HPACK decoding.</p>
 */
package dev.darcro.http2.frame;
